# to submit, use
#    qsub myjob

# to check results
#    qstat

cd $PBS_O_WORKDIR

# matlab -nodesktop -r "version"
# cat /proc/cpuinfo
# cat /proc/meminfo
# git clone https://github.com/GraphBLAS/LAGraph.git

cd $HOME/LAGraph/Test/BinRead
./dogap
