#include <bits/stdc++.h>

#define pb push_back
#define f first
#define s second

using namespace std;

int main(){
    ifstream data_in("grb_result.txt");
    set<int> edges;
    map<int, pair<int, int>> edge_map;
    while(!data_in.eof()){
        string s;
        getline(data_in, s);

    }
}